# Security & Credentials:
# - Do not hardcode cloud keys; use `bin/rails credentials:edit` or ENV vars.
# - Ensure public access is restricted on buckets unless specifically required.

test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>