<main class="flex-1 p-8 lg:p-12">
  <div class="max-w-5xl mx-auto">

    <div class="flex flex-col md:flex-row md:items-center justify-between gap-4 mb-10">
      <div>
        <h1 class="text-3xl font-extrabold text-slate-900 tracking-tight">School Directory</h1>
        <p class="text-slate-400 mt-1 text-base">Browse and manage registered partner schools.</p>
      </div>
      
      <div class="flex flex-col sm:flex-row items-center gap-3 w-full md:w-auto">
        <div class="relative w-full sm:w-80"> 
          <%= form_with url: schools_users_path, method: :get, class: "relative group" do |f| %>
            <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
              <svg class="h-5 w-5 text-slate-400 group-focus-within:text-blue-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
              </svg>
            </div>
            <%= f.text_field :query, 
                placeholder: "Search school or email...", 
                class: "block w-full pl-12 pr-4 py-3.5 border-2 border-slate-100 rounded-2xl bg-white text-sm font-medium placeholder-slate-400 focus:outline-none focus:ring-4 focus:ring-blue-500/10 focus:border-blue-500 transition-all shadow-md hover:shadow-lg",
                value: params[:query] %>
          <% end %>
        </div>

        <% if current_user.staff? %>
          <%= link_to new_school_users_path, class: "inline-flex items-center justify-center px-5 py-3.5 bg-blue-600 text-white text-sm font-bold rounded-2xl shadow-md shadow-blue-100 hover:bg-blue-700 transition-all shrink-0 w-full sm:w-auto" do %>
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
            </svg>
            <span>Add New School</span>
          <% end %>
        <% end %>
      </div>
    </div>

    <% if @schools.any? %>
      <div class="space-y-3.5">
        <%= render partial: "users/school_row", collection: @schools, as: :school %>
      </div>
    <% else %>
      <div class="text-center py-16 bg-slate-50 rounded-3xl border border-dashed border-slate-200">
        <p class="text-slate-400 font-medium">
          <%= params[:query].present? ? "No schools found matching '#{params[:query]}'." : "No schools have been registered yet." %>
        </p>
      </div>
    <% end %>

  </div>
</main>
